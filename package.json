{
  "name": "hosting-api",
  "version": "1.0.0",
  "description": "this is api in local",
  "main": "index.js",
  "nodemonConfig": {
    "ext": "json",
    "ignore": [
      "*.test.ts"
    ]
  
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js"
  },
  "author": "hieu tran",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
